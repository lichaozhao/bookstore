{
	"name": "bookstore-dev",
	"image": "mcr.microsoft.com/devcontainers/typescript-node:4-24-trixie",
	"workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}",
	"forwardPorts": [4000, 5173],
	"portsAttributes": {
		"4000": {
			"label": "backend",
			"onAutoForward": "notify"
		},
		"5173": {
			"label": "frontend",
			"onAutoForward": "openPreview"
		}
	},
	"postCreateCommand": "npm install && sudo npm exec --workspace frontend -- playwright install-deps chromium && npm exec --workspace frontend -- playwright install chromium",
	"postStartCommand": "test ! -d node_modules || npm exec --workspace frontend -- playwright install chromium",
	"customizations": {
		"vscode": {
			"settings": {
				"terminal.integrated.defaultProfile.linux": "bash"
			},
			"extensions": [
				"dbaeumer.vscode-eslint",
				"ms-vscode.vscode-typescript-next",
				"ms-playwright.playwright"
			]
		}
	},
	"remoteUser": "node"
}
